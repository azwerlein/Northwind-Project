@model Northwind.Models.Product

<div class="bg-primary m-1 p-1 text-white">
    <h4>Add Review</h4>
</div>
<form method="post" action="javascript:postReview()" id="reviewForm">

    <div hidden>
        <input hidden readonly type="text" value="@Model.ProductId" name="productID"/>
    </div>
    
    <div class="form-group">
        <label for="reviewText">Review: <span class="text-secondary">(Optional)</span></label>
        <textarea class="form-control" id="reviewText" name="reviewText" rows="3"></textarea>
    </div>
    
    @for (var i = 1; i <= 5; i++)
    {
        <div class="form-check form-check-inline">
            <input class="form-check-input" type="radio" name="rating" id="rating{@i}" value="@i">
            <label class="form-check-label" for="rating{@i}">@i</label>
        </div>
    }
    
    <button type="submit" class="btn btn-primary">Create Review</button>
</form>

<hr/>
<a asp-action="Index" asp-route-id="@Model.CategoryId">Home</a> | <a asp-action="Reviews" asp-route-id="@Model.ProductId">Reviews</a>

@section Scripts {
    <script src="/js/review.js"></script>
}